{{- include "ffc-helm-library.container-config-map" (list . "aiunit-tech-radar.config-map") -}}
{{- define "aiunit-tech-radar.config-map" -}}
data:
  NODE_ENV: {{ quote .Values.environment }}
  PORT: {{ quote .Values.container.port }}
  APPINSIGHTS_CLOUDROLE: {{ quote .Values.name }}
{{- end -}}
